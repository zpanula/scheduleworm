const { validateNewUser, validateUser } = require('./user-model.js');

describe('validateNewUser', () => {
  it.todo('write new test');
});

describe('validateUser', () => {
  it.todo('write new test');
});
